//usr/bin/env g++ "$0" -o /tmp/a; /tmp/a "$@"; exit $?
//extension: cpp

#include <iostream>

int main(int argc, char** args) {
    using namespace std;
    cout << "Hello, " << args[1] << "!" << endl;
    return 0;
}

